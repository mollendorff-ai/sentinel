"""Sentinel CLI entry point."""

import sys


def main() -> None:
    """Run Sentinel from the command line."""
    sys.stdout.write("Sentinel v0.1.0 — CLI entry point coming in v0.2.0\n")


if __name__ == "__main__":
    main()
